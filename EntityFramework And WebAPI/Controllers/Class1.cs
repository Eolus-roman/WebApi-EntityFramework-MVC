using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace EntityFramework_And_WebAPI.Controllers
{
    public class Class1
    {
         //if (d.Type == "Fridge")
         //   {
         //       IOpenOrClose iooc = (IOpenOrClose)d;
         //       IChangeEnum ice = (IChangeEnum)d;
         //   }
         //   if (d.Type == "Hoover")
         //   {
         //       IResetSettings irs = (IResetSettings)d;
         //       IUse iu = (IUse)GetDevice(type, id);
         //       IChangeEnum ice = (IChangeEnum)GetDevice(type, id);
         //   } 
         //   if (d.Type == "Bicycle")
         //   {
         //       ISpeed isp = (ISpeed)GetDevice(type, id);
         //       IResetSettings irs = (IResetSettings)GetDevice(type, id);
         //       IChangeEnum ice = (IChangeEnum)GetDevice(type, id);
         //   }
         //   if (d.Type == "Television")
         //   {
         //       IChangeEnum ice = (IChangeEnum)GetDevice(type, id);
         //       IVolume iv = (IVolume)GetDevice(type, id);
         //   }
         //   if (d.Type == "Warhammer")
         //   {
         //       IChangeEnum ice = (IChangeEnum)GetDevice(type, id);
         //       IUse iu = (IUse)GetDevice(type, id);
         //   }
    }
}